# Sync bug triage

This register is correctness-first. P0/P1 entries require an automated, red-capable repro before a code change. P2 items stay isolated from protocol changes.

| ID          | Priority | Expected / actual                                                                                                                                                                                            | Minimal reproduction                                                   | Client                             | Evidence                                                                                             | Status |
| ----------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------- | ---------------------------------- | ---------------------------------------------------------------------------------------------------- | ------ |
| SYNC-P0-001 | P0       | A pull response whose cursor order regresses must be rejected without advancing the replica; instead Sync Core applied both changes and could leave a stale payload as canonical state.                      | Return `j:2` then stale `j:1` for the same content.                    | Shared Sync Core (Desktop and Web) | `tests/e2e/harness/sync-protocol.harness.test.ts` — regression test passes                           | Fixed  |
| SYNC-P0-002 | P0       | If durable outbox persistence fails, no optimistic mutation may become visible; instead Sync Core wrote the optimistic replica first, then an outbox failure left a local-only edit with no future delivery. | Force `Outbox.enqueue` to reject and inspect the user-visible replica. | Shared Sync Core (Desktop and Web) | `tests/e2e/harness/sync-protocol.harness.test.ts` — regression test passes                           | Fixed  |
| SYNC-P2-001 | P2       | Desktop sync service regression tests should run in the standard Bun test command; instead module initialization failed before the test body because Electron's runtime-only `shell` export was unavailable. | `bun test apps/desktop/src/main/desktop-sync.service.test.ts`          | Desktop developer/test UX          | Passing regression test after injected `openExternal` dependency seam                                | Closed |
| SYNC-P1-001 | P1       | A client must pull again after establishing realtime so a write in the subscribe race window is observed; instead it could miss the hint and remain stale until a later trigger.                             | Publish a remote change during `RealtimeTransport.connect`.            | Shared Sync Core (Desktop and Web) | `tests/e2e/harness/sync-protocol.harness.test.ts` — regression test passes                           | Fixed  |
| SYNC-P2-002 | P2       | Web E2E should provision an available backend port and browser driver; instead it could not reach browser assertions.                                                                                        | `bun run test:e2e:web`                                                 | Web developer/test UX              | Passed after documented `@puppeteer/browsers install chromedriver@150.0.7871.188 --path .cache/wdio` | Closed |
| SYNC-P1-002 | P1       | A failed initial sync start must be retryable; instead `start()` marked the engine started before its initial pull and the next `start()` silently no-oped.                                                  | Fail the first pull, then retry `start()`.                             | Shared Sync Core (Desktop and Web) | `tests/e2e/harness/sync-protocol.harness.test.ts` — regression test passes                           | Fixed  |
| SYNC-P2-003 | P2       | Desktop E2E must reach UI assertions; instead Electron main process exited before the window because the ChromeDriver-launched process inherited a macOS-inaccessible workspace cwd.                         | `bun run test:e2e:desktop`                                             | Desktop E2E runner                 | Passed after running WDIO/ChromeDriver from `/tmp` with absolute config paths                        | Closed |

## P2 backlog

| ID          | Expected / actual | Evidence                                                      | Status |
| ----------- | ----------------- | ------------------------------------------------------------- | ------ |
| SYNC-P2-001 | See triage table. | `bun test apps/desktop/src/main/desktop-sync.service.test.ts` | Closed |
| SYNC-P2-002 | See triage table. | `bun run test:e2e:web`                                        | Closed |
| SYNC-P2-003 | See triage table. | `bun run test:e2e:desktop`                                    | Closed |
